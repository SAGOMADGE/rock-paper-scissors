/*
Псевдокод для Rock/Paper/Scissors

/* 
Создаем функцию getComputerChoice 

Внутри этой функции задаем переменную randomNumber

К этой переменной приравниваем значение выбора компьютера с помощью MathFloor(Math.random()*3) - 0,1,2

Создаем условие if/else с тремя исходами 
1) Если randomNumber = 0, то возвращаем "rock"
2) Если randomNumber = 1, то возвращаем "scissors"
3) Если randomNumber = 2, то возвращаем "paper"


Создаем функцию getHumanChoice() 

Задаем внутри переменную humanChoice

Задаем и приравниваем к переменной результат prompt("Сделай свой выбор") 
Приравниваем humanChoice к HumanChoice.toLowerCase() ;
Возвращаем значение переменной 
Закрываем функцию


Вне функции глобально задаем две переменные для ведения счета и приравниваем их к нулю
humanScore = 0
computerScore = 0


Далее создаем ф-ию playround
В скобках функции задаем 2 параметра - humanChoice ,  computerChoice
Параметры будут case-insensutive

Задаем условия для игры:
Если hC равно cC вывести "it's a tie"

Иначе если hC равно "rock" И cC равно "scissors" вывести "You win. Rock beats scissors"

Иначе если hC равно "rock" И cC равно "paper" вывести "You lose. Paper beats rock"

Иначе если hC равно "paper" И cC равно "rock" вывести "You win. Paper beats rock"

Иначе если hC равно "paper" И cC равно "scissors" вывести "You lose. Paper beats scissors"

Иначе если hC равно "scissors" И cC равно "paper" вывести "You win. Scissors beats paper" 

Иначе если hC равно "scissors" И cС равно "rock" вывести "You lose. Rock beat scissors"
Закрываем функцию

Задаем две переменные в которых будем хронить выбор человека и компьютера исходят из двух функций в которых этот выбор происходил 

const humanSelection =  getHumanChoice();
const computerSelection = getHumanChoice();

Далее мы закидываем результаты этих двух функций значения которых принимают выше заданные переменные как аргументы в нашу функцию playRound, внутри которой происходит логика if/else и определяется победительПсевдокод для Rock/Paper/Scissors

/* 
Создаем функцию getComputerChoice 

Внутри этой функции задаем переменную randomNumber

К этой переменной приравниваем значение выбора компьютера с помощью MathFloor(Math.random*3) - 0,1,2

Создаем условие if/else с тремя исходами 
1) Если randomNumber = 0, то возвращаем "rock"
2) Если randomNumber = 1, то возвращаем "scissors"
3) Если randomNumber = 2, то возвращаем "paper"


Создаем функцию getHumanChoice() 

Задаем внутри переменную humanChoice

Задаем и приравниваем к переменной результат prompt("Сделай свой выбор") 
Возвращаем значение переменной 
Закрываем функцию


Вне функции глобально задаем две переменные для ведения счета и приравниваем их к нулю
humanScore = 0
computerScore = 0


Далее создаем ф-ию playround
В скобках функции задаем 2 параметра - humanChoice ,  computerChoice
Параметры будут case-insensutive

Задаем условия для игры:
Если hC равно cC вывести "it's a tie"

Иначе если hC равно "rock" И cC равно "scissors" Console.log"You win. ${humanChoise} beats ${computerChoice}"
humanScore++;

Иначе если hC равно "rock" И cC равно "paper" вывести "You lose. Paper beats rock"

Иначе если hC равно "paper" И cC равно "rock" вывести "You win. Paper beats rock"

Иначе если hC равно "paper" И cC равно "scissors" вывести "You lose. Paper beats scissors"

Иначе если hC равно "scissors" И cC равно "paper" вывести "You win. Scissors beats paper" 

Иначе если hC равно "scissors" И cС равно "rock" вывести "You lose. Rock beat scissors"
Закрываем функцию

Задаем две переменные в которых будем хрaнить выбор  исходя из двух функций в которых этот выбор происходил 

const humanSelection =  getHumanChoice();
const computerSelection = getComputerChoice();

Далее мы закидываем результаты этих двух функций значения которых принимают выше заданные переменные как аргументы в нашу функцию playRound, внутри которой происходит логика if/else и определяется победитель



Игра состоит из пяти раундов, для этого нам нужно пять раз вызвать новую функцию playGame и  вызывать переменные getHumanChoice и getComputerChouce внутри функции чтоб значения обнулялись

Далее сравниваем счет компьютера и человека через условия:

Если humanScore > computerScore вывести "Game over! " You won! ${humanScore} - ${computerScore}"

Иначе если  humanScore < computerScore вывести "Game over! " You lost! ${humanScore} - ${computerScore}"

Иначе  вывести "Game over! " It's a tie! ${humanScore} - ${computerScore}"
*/